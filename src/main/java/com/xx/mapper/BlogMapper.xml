<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xx.mapper.BlogMapper">
    <select id="getMyStarList" parameterType="map" resultType="BlogView">
        select *
        from `blog`
        where `id` in (select `blog_id` from `blog_star` where `logic_delete` = 0 and `username` = #{username})
        limit #{startIndex}, #{pageSize};
    </select>

    <select id="getMyUpList" parameterType="map" resultType="BlogView">
        select *
        from `blog`
        where `id` in (select `blog_id` from `blog_up` where `logic_delete` = 0 and `username` = #{username})
        limit #{startIndex}, #{pageSize};
    </select>

    <select id="getMyDownList" parameterType="map" resultType="BlogView">
        select *
        from `blog`
        where `id` in (select `blog_id` from `blog_down` where `logic_delete` = 0 and `username` = #{username})
        limit #{startIndex}, #{pageSize};
    </select>

    <select id="getBlogListByCategories" parameterType="map" resultType="Blog">
        select *
        from `blog`,
             `category_blog`
        where `category_blog`.`category_name` in #{categories}
        order by #{orderby}
        limit #{startIndex}, #{pageSize};
    </select>
</mapper>