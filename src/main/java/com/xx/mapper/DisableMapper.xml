<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xx.mapper.DisableMapper">
    <insert id="setUserDisableTime" parameterType="map">
        insert into disable(username, end_time, reason)
        values (#{username}, date_add(current_timestamp(), interval #{timestamp} second), #{reason});
    </insert>
</mapper>